---
description: Set the text that Claudergency appends to every prompt
---

Set the Claudergency config text. Write the text "$ARGUMENTS" to the file at ~/.claude/claudergency.txt (create the directory if needed). Confirm to the user what was saved.

If no arguments were provided, tell the user the usage: `/claudergency:set Your custom text here`
